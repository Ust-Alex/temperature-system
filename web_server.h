/**
 * ============================================================================
 * @file web_server.h
 * @brief ВЕБ-СЕРВЕР И WEBSOCKET
 * @version 6.2
 * 
 * Содержит функции для запуска и обработки веб-сервера и WebSocket.
 * ============================================================================
 */

#ifndef WEB_SERVER_H
#define WEB_SERVER_H

#include <Arduino.h>
#include <WebSocketsServer.h>
#include <ESPAsyncWebServer.h>

// ============================================================================
// ГЛОБАЛЬНЫЕ ОБЪЕКТЫ
// ============================================================================
extern WebSocketsServer webSocket;
extern AsyncWebServer server;
extern bool wifiClientConnected;

// ============================================================================
// ФУНКЦИИ ИНИЦИАЛИЗАЦИИ
// ============================================================================

/**
 * @brief Инициализация веб-сервера (LittleFS, обработчики)
 */
void initWebServer();

/**
 * @brief Инициализация WebSocket (порт, обработчик событий)
 */
void initWebSocket();

#endif // WEB_SERVER_H