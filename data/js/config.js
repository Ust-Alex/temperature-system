/**
 * ============================================================================
 * @file config.js
 * @brief КОНФИГУРАЦИЯ ВЕБ-ИНТЕРФЕЙСА
 * @version 6.2
 * 
 * Содержит все настройки: точки, диапазоны, цвета, имена датчиков
 * ============================================================================
 */

// ============================================================================
// 1. КОНФИГУРАЦИЯ
// ============================================================================
const CONFIG = {
    MAX_POINTS: 7200,
    WS_URL: 'ws://' + window.location.hostname + ':8080',
    MEASURE_INTERVAL: 1166,
    CHART_SYNC_INTERVAL: 1000,
    FAST_UPDATE_INTERVAL: 500,
    RANGES: {
        '1ч': 3600,
        '30м': 1800,
        '15м': 900,
        '5м': 300,
        '1м': 60
    },
    FAST_UPDATE_RANGES: [60],
    RECONNECT: {
        MAX_ATTEMPTS: 5,
        MAX_TOTAL_ATTEMPTS: 20,
        DELAYS: [1, 2, 4, 8, 15, 30, 30, 30, 30, 30],
        WATCHDOG_INTERVAL: 5000,
        BASE_DELAY: 2000        // Начальная задержка переподключения (мс)
    },
    WATCHDOG: {
        PING_INTERVAL: 10000,
        DATA_TIMEOUT: 15000
    },
    ZOOM_LIMITS: {
        MIN_TEMP: 20,
        MAX_TEMP: 90,
        MIN_RANGE: 0.1
    },
    DATASET_NAMES: ['Выход', '100см', '75см', '50см', 'Гильза', 'Куб'],
    DATASET_COLORS: ['#FF00FF', '#FFA500', '#00FFFF', '#FFFF00', '#00FF00', '#FF4500']
};