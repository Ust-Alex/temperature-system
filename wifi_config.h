/**
 * ============================================================================
 * @file wifi_config.h
 * @brief КОНФИГУРАЦИЯ Wi-Fi (ВСЕ НАСТРОЙКИ В ОДНОМ МЕСТЕ)
 * @version 6.2
 * 
 * Здесь собраны все параметры, которые могут меняться пользователем:
 * - Две сети для подключения (SSID / пароль)
 * - Статические IP для каждой сети
 * - Параметры ретраев (количество попыток, таймауты)
 * - Настройки режима точки доступа (AP)
 * ============================================================================
 */

#ifndef WIFI_CONFIG_H
#define WIFI_CONFIG_H

#include <IPAddress.h>

// ============================================================================
// 1. ДВЕ СЕТИ ДЛЯ ПОДКЛЮЧЕНИЯ (РОУТЕРЫ)
// ============================================================================

// ----- ПЕРВАЯ СЕТЬ (cherven30_TP-Link, 192.168.1.x) -----
#define MY_SSID_1 "cherven30_TP-Link"   // Имя сети (SSID)
#define MY_PASS_1 "Bb_00010121"        // Пароль

// ----- ВТОРАЯ СЕТЬ (Xiaomi_UST, 192.168.100.x) -----
#define MY_SSID_2 "Xiaomi_UST"          // Имя сети (SSID)
#define MY_PASS_2 "1456654123"        // Пароль

// ============================================================================
// 2. СТАТИЧЕСКИЕ IP ДЛЯ КАЖДОЙ СЕТИ
// ============================================================================

// Для первой сети (cherven30_TP-Link)
#define STA1_IP     192, 168, 1, 100    // IP для ESP32
#define STA1_GATEWAY 192, 168, 1, 1     // Шлюз (роутер)
#define STA1_SUBNET 255, 255, 255, 0    // Маска подсети

// Для второй сети (Xiaomi_UST)
#define STA2_IP     192, 168, 100, 100  // IP для ESP32
#define STA2_GATEWAY 192, 168, 100, 1   // Шлюз (роутер)
#define STA2_SUBNET 255, 255, 255, 0    // Маска подсети

// ============================================================================
// 3. ПАРАМЕТРЫ РЕТРАЕВ (активных попыток подключения)
// ============================================================================

#define STA_RETRY_ATTEMPTS 3            // Количество попыток на одну сеть
#define STA_ATTEMPT_TIMEOUT_MS 5000     // Таймаут одной попытки (мс)
#define STA_RETRY_DELAY_MS 1000         // Пауза между попытками (мс)
#define WIFI_RECONNECT_INTERVAL_MS 30000     // Интервал проверки связи (мс)

// ============================================================================
// 4. НАСТРОЙКИ РЕЖИМА AP (точка доступа)
// ============================================================================

#define AP_SSID "TermoESP32"            // Имя точки доступа
#define AP_PASSWORD ""                  // Пароль (пустой = открытая сеть)
#define AP_CHANNEL 6                    // Канал Wi-Fi
#define AP_IP 192, 168, 4, 1            // IP-адрес точки доступа
#define AP_GATEWAY 192, 168, 4, 1       // Шлюз
#define AP_SUBNET 255, 255, 255, 0      // Маска подсети
#define AP_TX_POWER 80                  // 80 = 20 дБм (максимальная мощность)

// ============================================================================
// 5. НАСТРОЙКИ ВЕБ-СЕРВЕРА
// ============================================================================

#define WEBSOCKET_PORT 8080             // Порт для WebSocket

#endif // WIFI_CONFIG_H