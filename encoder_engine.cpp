/**
 * ============================================================================
 * ФАЙЛ: encoder_engine.cpp
 * РЕАЛИЗАЦИЯ РАБОТЫ С ЭНКОДЕРОМ KY-040 (ПОЛНАЯ ПОДДЕРЖКА GYVER)
 * 
 * ВЕРСИЯ: 4.0 (ПЕРЕПИСАН ПОД БИБЛИОТЕКУ ENCBUTTON)
 * 
 * ОСОБЕННОСТИ:
 * 1. Использует библиотеку EncButton для надёжной обработки
 * 2. Отправляет ВСЕ события в очередь без ранних выходов
 * 3. Поддерживает: поворот, поворот с удержанием, клик, двойной клик, удержание
 * 4. Таймер неактивности пользователя
 * ============================================================================
 */

#include "encoder_engine.h"
#include "system_config.h"
#include <EncButton.h>

// ============================================================================
// ЛОКАЛЬНЫЕ ПЕРЕМЕННЫЕ (STATIC - ВИДНЫ ТОЛЬКО В ЭТОМ ФАЙЛЕ)
// ============================================================================

// Объект энкодера из библиотеки EncButton
// Параметры: CLK, DT, SW, режим пинов энкодера (INPUT), режим пина кнопки (INPUT_PULLUP)
EncButton enc(ENCODER_CLK, ENCODER_DT, ENCODER_SW, INPUT, INPUT_PULLUP);

// Время последней активности пользователя (для таймаута)
static uint32_t lastActivityTime = 0;

// Очередь событий (внешняя, объявлена в globals.h)
extern QueueHandle_t eventQueue;

// ============================================================================
// ИНИЦИАЛИЗАЦИЯ ЭНКОДЕРА
// ============================================================================
void encoder_init() {
    Serial.println("[ENCODER] Инициализация энкодера...");
    
    // НАСТРОЙКА УРОВНЯ СИГНАЛА КНОПКИ
    // LOW - кнопка замыкает на GND (активный низкий уровень)
    enc.setBtnLevel(LOW);
    
    // НАСТРОЙКА ТАЙМАУТОВ (в миллисекундах)
    enc.setClickTimeout(500);   // Макс. время между кликами для двойного нажатия
    enc.setDebTimeout(50);      // Время гашения дребезга контактов
    enc.setHoldTimeout(400);    // Время до срабатывания удержания (400 мс)
    enc.setStepTimeout(200);    // Период импульсов при удержании
    enc.setTimeout(1000);       // Общий таймаут действия
    
    // НАСТРОЙКА ЭНКОДЕРА (декодера)
    enc.setEncReverse(0);       // 0 - нормальное направление, 1 - реверс
    enc.setEncType(EB_STEP4_LOW); // Тип сигнала: 4 фазы за щелчок, активный LOW
    enc.setFastTimeout(30);     // Таймаут быстрого поворота
    
    // Сброс счётчика энкодера
    enc.counter = 0;
    
    // Инициализация таймера неактивности
    lastActivityTime = millis();
    
    Serial.println("[ENCODER] Энкодер инициализирован");
    Serial.printf("[ENCODER] Пины: CLK=%d, DT=%d, SW=%d\n", 
                  ENCODER_CLK, ENCODER_DT, ENCODER_SW);
    Serial.printf("[ENCODER] Таймаут удержания: 400 мс\n");
    Serial.printf("[ENCODER] Таймаут клика: 500 мс\n");
}

// ============================================================================
// ОПРОС ЭНКОДЕРА - ВЫЗЫВАТЬ КАЖДЫЕ 10-20 МС
// ============================================================================
void encoder_tick() {
    // Обязательно вызываем tick() для опроса библиотеки
    enc.tick();
    
    // ========================================================================
    // ШАГ 1: СБРОС ТАЙМЕРА НЕАКТИВНОСТИ ПРИ ЛЮБОМ ДЕЙСТВИИ
    // ========================================================================
    if (enc.action()) {
        lastActivityTime = millis();
    }
    
    // ========================================================================
    // ШАГ 2: ПРОВЕРЯЕМ ВСЕ СОБЫТИЯ ПОДРЯД (БЕЗ РАННИХ ВОЗВРАТОВ)
    // ========================================================================
    
    // 2.1. Поворот влево
    if (enc.left()) {
        EncoderEvent_t ev = enc.hold() ? EVENT_HOLD_LEFT : EVENT_ENCODER_LEFT;
        if (eventQueue != NULL) {
            xQueueSend(eventQueue, &ev, 0);
        }
    }
    
    // 2.2. Поворот вправо
    if (enc.right()) {
        EncoderEvent_t ev = enc.hold() ? EVENT_HOLD_RIGHT : EVENT_ENCODER_RIGHT;
        if (eventQueue != NULL) {
            xQueueSend(eventQueue, &ev, 0);
        }
    }
    
    // 2.3. Клик (одинарный или двойной)
    if (enc.click()) {
        EncoderEvent_t ev = enc.hasClicks(2) ? EVENT_BUTTON_DOUBLE : EVENT_BUTTON_CLICK;
        if (eventQueue != NULL) {
            xQueueSend(eventQueue, &ev, 0);
        }
    }
    
    // 2.4. Начало удержания (без поворота)
    if (enc.hold() && !enc.left() && !enc.right()) {
        EncoderEvent_t ev = EVENT_BUTTON_HOLD;
        if (eventQueue != NULL) {
            xQueueSend(eventQueue, &ev, 0);
        }
    }
    
    // 2.5. Отпускание после удержания (опционально)
    if (enc.release()) {
        // Можно добавить событие отпускания, если нужно
        // EncoderEvent_t ev = EVENT_BUTTON_RELEASE;
        // xQueueSend(eventQueue, &ev, 0);
    }
    
    // 2.6. Импульсное удержание (step) — для повторов при долгом нажатии
    if (enc.step()) {
        // Используется для автоповтора, пока не нужно
    }
}

// ============================================================================
// СБРОС ТАЙМЕРА НЕАКТИВНОСТИ
// ============================================================================
void encoder_reset_inactivity_timer() {
    lastActivityTime = millis();
}

// ============================================================================
// ПОЛУЧЕНИЕ ВРЕМЕНИ НЕАКТИВНОСТИ (В МИЛЛИСЕКУНДАХ)
// ============================================================================
uint32_t encoder_get_inactivity_time() {
    return millis() - lastActivityTime;
}